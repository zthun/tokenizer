version: ${version}
protocol: ${protocol},
domain: ${domain},
port: ${port},
subdomain: ${subdomain},
path: ${path},
hash: ${hash},
secure: ${secure}
url: ${protocol}://${subdomain}.${domain}:${port}/${path}#${hash}
